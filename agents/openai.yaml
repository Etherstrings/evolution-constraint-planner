interface:
  display_name: "Evolution Constraint Planner v2"
  short_description: "Strict ECL pre/plan/code/achieve delivery"
  default_prompt: "Use $evolution-constraint-planner to turn a raw product or engineering request into a Stage A approval workspace, then a frozen code-ready handoff, execute only from that handoff, and verify the delivered result with the strict pre/plan/code/achieve contract."
